# Terraform
*.tfstate
*.tfstate.backup
*.tfstate.*
.terraform/
.terraform.lock.hcl
terraform.tfvars
*.auto.tfvars
# Per-environment var files contain secrets — never commit. Templates (*.tfvars.example) are safe.
*.tfvars
!*.tfvars.example
crash.log
crash.*.log
# Saved plan files (binary, may embed sensitive values)
tfplan
*.tfplan
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Account-specific backend configs (do not commit — contain the account ID)
backend.hcl
backend-*.hcl
backend.hcl.pre-adr-0028.bak
!backend-*.hcl.example
