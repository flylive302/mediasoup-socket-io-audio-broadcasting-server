# Terraform S3 Backend — staging environment (ADR 0028: key-per-environment).
# See backend-production.hcl.example for full instructions; only the key differs.
# Generated by scripts/aws/bootstrap-state.sh. Gitignored once copied.

bucket = "flylive-audio-tfstate-YOUR_ACCOUNT_ID_HERE"
key    = "env/staging/terraform.tfstate"
