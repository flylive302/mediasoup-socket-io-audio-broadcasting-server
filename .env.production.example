NODE_ENV=production
PORT=3030
LOG_LEVEL=info

# Redis (Digital Ocean Valkey / Managed Redis)
REDIS_HOST=your-redis-host
REDIS_PORT=25061
REDIS_DB=3
REDIS_PASSWORD=your-redis-password
REDIS_TLS=true

# Laravel Integration
LARAVEL_API_URL=https://backend-laravel-12-master-txvbmd.laravel.cloud
LARAVEL_INTERNAL_KEY=your-secure-internal-key

# MediaSoup
# In production with Docker/Kubernetes, these often need specific public IPs
MEDIASOUP_LISTEN_IP=0.0.0.0
MEDIASOUP_ANNOUNCED_IP=your-droplet-public-ip

# SSL (If handling SSL termination on app, otherwise leave commented if using Load Balancer)
# SSL_KEY_PATH=/path/to/privkey.pem
# SSL_CERT_PATH=/path/to/fullchain.pem

# CORS
# Comma-separated list of allowed origins
CORS_ORIGINS=https://besttvcableinternet.us,https://www.besttvcableinternet.us,wss://www.besttvcableinternet.us
